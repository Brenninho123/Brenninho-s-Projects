import turtle
import print
import software
import auto

def draw_square(t, size):
    
    for _ in range(4):
        t.forward(size)
        t.backward(size)
        t.right(90)
        t.left(90)
        t.backward(120)
        t.forward(300)
        
        print='Hello World'
        print='i am Turtle'

def software(auto):

    for _ in auto(size):
        s.left()
        s.up()
        s.right
        s.down
        
